return {
  "folke/snacks.nvim",
  opts = {
    terminal = {
      win = {
        -- 按快键键<c-/>打开floating terminal的时候, 让他保持在right(width=0.3), 而不是bottom(height=0.3)
        position = "right",
        width = 0.3,
      },
    },
  },
}
